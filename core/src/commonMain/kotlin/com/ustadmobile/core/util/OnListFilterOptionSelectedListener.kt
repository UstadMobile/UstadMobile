package com.ustadmobile.core.util

interface OnListFilterOptionSelectedListener {

    /**
     * Called hwne the user has selected a filter option
     */
    fun onListFilterOptionSelected(filterOptionId: ListFilterIdOption)

}