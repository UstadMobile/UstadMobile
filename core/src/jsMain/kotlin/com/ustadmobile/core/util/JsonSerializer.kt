package com.ustadmobile.core.util

//import io.ktor.client.features.json.serializer.*

/**
 * Set default serializer to be used, without setting this it will
 * throw default serializer exception saying list list is empty.
 *
 * NOTE: This should be called before executing any code, on web app call it
 * on main before window.onload
 */
fun defaultJsonSerializer() = println("")

