NodeJS Testing SERVER:

The server can receive results over http (e.g. when an app runs on a device)
and can serve assets over http.  When serving assets it can slow downoads and
break them to test resume functionalities in the app..

To install required node modules:

./configure.sh

