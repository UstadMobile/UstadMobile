package com.ustadmobile.lib.db;

public class TestScheduledCheckDao {

    /*
    TO insert from
            List<ScheduledCheck> check = serverDb.getScheduledCheckDao().findByCheckTypeAndArgs(
                ScheduledCheck.TYPE_RECORD_ATTENDANCE_REMINDER);
        Assert.assertEquals("Created one scheduled check", 1, check.size());
        String expectedParams = ScheduledCheck.PARAM_CLAZZ_LOG_UID + "=" + allClazzLogs.get(0)
                .getClazzLogUid();
        Assert.assertEquals("Scheduled check clazzLogUid = created clazz log uid",
                expectedParams, check.get(0).getCheckParameters());

     */

}
