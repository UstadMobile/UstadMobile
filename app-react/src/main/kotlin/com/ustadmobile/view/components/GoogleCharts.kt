package com.ustadmobile.view.components

/*
import react.RBuilder
import react.RComponent
import react.RState
import styled.StyledHandler
import styled.StyledProps

@JsModule("react-google-charts")
@JsNonModule
private external val googleChartsModule: dynamic

@Suppress("UnsafeCastFromDynamic")
private val googleChartsComponent: RComponent<GoogleChartsProps, RState> = googleChartsModule.default

external interface GoogleChartsProps: StyledProps {
    var width: String
    var height: String
    var chartType: String
    var data: Array<Array<Any>>
    var options: Any?
}

enum class MChartType(val chartType: String){
    AreaChart("AreaChart"),
    ColumnChart("ColumnChart"),
    LineChart("LineChart")
}

fun RBuilder.mChart(data: Array<Array<Any>>,
                    width: Any? = "100%",
                    height: Any? = "400px",
                    chartType: MChartType = MChartType.ColumnChart,
                    options: Any? = null,
                    className: String? = null,
                    handler: StyledHandler<GoogleChartsProps>) = createStyled(googleChartsComponent) {
    attrs.width = width.toString()
    attrs.height = height.toString()
    attrs.chartType = chartType.toString()
    attrs.data = data
    attrs.options = options
    setStyledPropsAndRunHandler(className, handler)
}*/
