appId: com.android.chrome

# Updates Google Chrome - see README.md
---
- launchApp
- runFlow:
    when:
      visible: "Update"
    file: update-dialog.yaml
- launchApp
- assertNotVisible: "Update"
- runFlow:
    when:
      visible: "Use without an account"
    file: "accept-without-account.yaml"
