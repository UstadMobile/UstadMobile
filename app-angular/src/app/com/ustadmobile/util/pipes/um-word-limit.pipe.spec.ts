import { UmWordLimitPipe } from './um-word-limit.pipe';

describe('UmWordLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new UmWordLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
