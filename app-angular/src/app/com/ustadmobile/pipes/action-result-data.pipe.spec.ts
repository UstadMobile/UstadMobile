import { ActionResultDataPipe } from './action-result-data.pipe';

describe('ActionResultDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionResultDataPipe();
    expect(pipe).toBeTruthy();
  });
});
