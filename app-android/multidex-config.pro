# We need to use this when we want to run tests on Android 4.4 if we can get the method count down
#-keep public class com.ustadmobile.port.android.view.ClazzEditFragmentTest { *; }

#-keep class org.junit.**
